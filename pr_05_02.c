#include<stdio.h>
#include<math.h>
#include<string.h>
int check(string1, string2);
int main()
{ 
    printf("Enter the string:");
    char string1[20];
    scanf(" %[^\n]",string1);

    printf("Enter string to be checked:");
    char string2[20];
    scanf(" %[^\n]",string2);

    int x = check(string1, string2);

    return 0;
}

int check(string1, string2)
{   
    for(int i=0; i<n1; i++)
    {
        if()
    }

    // if(*string2==*string1)
    //  return check(*string2==*string1)
    // else
    // return 0;
}